# pytest cache directory #
# Название вашего проекта

Краткое описание: что делает этот проект, какую проблему решает.

## 🚀 Установка и запуск

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://gitlab.com/ваш_username/ваш_project_name.git
    cd ваш_project_name
    ```

2.  **(Рекомендуется) Создайте и активируйте виртуальное окружение:**
    ```bash
    python -m venv venv
    # Для Windows:
    venv\Scripts\activate
    # Для Linux/Mac:
    source venv/bin/activate
    ```

3.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

4.  **Настройте проект:**
    *   Если нужны переменные окружения, скопируйте файл `.env.example` в `.env` и заполните своими значениями.
    *   Укажите пути к данным и т.п.

5.  **Запустите приложение:**
    ```bash
    python src/main.py
    ```
    Или, если это веб-приложение:
    ```bash
    flask run
    ```

## 📁 Структура проекта
saucedemo/
├── drivers/
│   ├── chromedriver.exe
├── pages/
│   ├── __init__.py
│   ├── base_page.py
│   └── login_page.py
│   └── main_page.py
├── tests/
│   ├── __init__.py
│   └── test_login.py
├── conftest.py
├── requirements.txt